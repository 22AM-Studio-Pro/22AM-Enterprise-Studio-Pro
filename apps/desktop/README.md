#!/usr/bin/env sh

# This file is intentionally small. The frontend is a Vite app.
# The Tauri backend is configured under src-tauri/.

echo "Run 'pnpm install' at repo root, then 'pnpm dev' to start the desktop app UI."
